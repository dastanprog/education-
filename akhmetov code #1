import math

def calculate_sum(terms=100):
    total_sum = 0
    for n in range(1, terms + 1):
        term = (n + 1) / (2**n * math.factorial(n - 1))
        total_sum += term
    return total_sum
result = calculate_sum()
print(result)
